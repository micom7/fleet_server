# auto_telemetry — шаблон змінних оточення для сервера
# Скопіювати в .env і замінити значення:
#   cp .env.example .env

# PostgreSQL (для Docker — пароль суперюзера)
POSTGRES_PASSWORD=change_me_strong_password

# Python-сервіси підключаються до postgres-контейнера
DB_HOST=postgres
DB_PORT=5432
DB_NAME=telemetry
DB_USER=telemetry
DB_PASSWORD=change_me_strong_password

# Outbound API — цей ключ потрібно вписати у fleet_server admin (поле api_key авто)
OUTBOUND_API_KEY=change_me_strong_api_key

# Назва авто (відображається у fleet_server при GET /status)
VEHICLE_ID_HINT=sim-vehicle-01

# Інтервал запису вимірювань (секунди, за замовч. 2)
# WRITE_INTERVAL_SEC=2

# Інтервал виводу статистики в лог (секунди, за замовч. 60)
# LOG_INTERVAL_SEC=60

# ZeroMQ — не використовується симулятором, але потрібен outbound для /status
ZMQ_COLLECTOR_PUB=tcp://127.0.0.1:5555
